<p id="notice"><%= notice %></p>

<h1>Projects</h1>

  <% @projects.each_with_index do |project, i| %>
      <% if(project.accounts.present?) %>
        <%= link_to image_tag(project.primary_account.display_picture_url, class: 'img-rounded project-thumb'),project %>
      <% else %>
        <%= link_to project do %>
          <i class="fa fa-folder fa-4x img-rounded project-thumb"></i>
        <% end %>
      <% end %>
   <!--  <tr>
      <td><%= project.name %></td>
      <td><%= project.slug %></td>
      <td><%= project.trello_board_uid %></td> -->
     <!--  <td><%= link_to 'Show', project %></td>
      <td><%= link_to 'Edit', edit_project_path(project) %></td>
      <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
    </tr>
  <% end %>

<br>

<%= link_to 'New Project', new_project_path %>
